import type { NextPage } from 'next';

import CardsOverviewContainer from '@components/cards/overview/CardsOverviewContainer';

const Home: NextPage = () => {
  return <CardsOverviewContainer />;
};

export default Home;
