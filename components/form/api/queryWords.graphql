query WordListById($request: WordListInput!) {
    wordListById(request: $request) {
        items {
            id
            name
            description
            words {
                id
                original
                translation
            }
        }
    }
}
